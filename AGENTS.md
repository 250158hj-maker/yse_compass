# YSE Compass — リポジトリコンテキスト

卒業制作チーム **Cheers**（水戸・蒲山・鈴木）が開発する、**卒業制作発表会の運営プラットフォーム**。
資料の提出という行為が、提出管理・発表順整理・アーカイブを副産物として成立させることを狙う。

---

## 1. 正典マップ — どこに何の正典があるか

| 対象 | 正典 | 読む人 |
| --- | --- | --- |
| **設計の詳細**（スコープ・機能要件・非機能・制約・語彙・画面・決定の履歴） | **このリポジトリの `docs/`** | チーム3名＋各自の Claude Code |
| プロダクト定義・機能一覧・スケジュール（Ph.0・**凍結**） | Notion `YSE COMPASS COMP.` の企画書 | 全員・先生 |
| プロジェクトの現在地・今週のタスク | Notion トップページ `YSE Compass` | 全員・先生 |
| 検討の過程（なぜそう考えたか・捨てた案・根拠の長文） | 水戸個人の Obsidian | 水戸 |

> **設計に関する問いの答えは `docs/` にある。**
> Notion に同じ内容が見つかった場合、**そちらが古い**。`docs/` を正とし、矛盾を `docs/open-questions.md` に記録すること。
>
> 2026-08-30 変更：それ以前は Notion「要件定義書」が正典だった。変更の理由は `docs/decisions.md` の同日の行を参照。

---

## 2. まず読むファイル（作業の種類ごと）

| やること | 読む順 |
| --- | --- |
| ER・テーブル定義・API 設計 | `docs/glossary.md` → `docs/requirements.md` → `docs/open-questions.md` → `docs/design/06-data.md` |
| 画面・モック・UI | `docs/design/04-screen.md` → `docs/requirements.md` |
| **設計書を書く・読む** | `docs/design/00-conventions.md`（書き方の規約と未決マップ）→ 該当章 |
| **実装しようとして決まっていなかった** | `docs/findings.md` に書く → `docs/open-questions.md` にも1件立てる |
| 「作るか作らないか」の判断 | `docs/decisions.md` → `docs/requirements.md` §1 |
| 「なぜこうなっているか」を知りたい | `docs/decisions.md`（根拠の列） |
| 先生の実運用・一次情報を知りたい | `docs/hearing.md` |
| **スケジュール・タスクの割り付け** | `pm/README.md` → `pm/wbs-source.md` |

全ファイルの一覧と役割は **`docs/README.md`**。**進め方（スケジュール・WBS）の文書は `pm/README.md`。**

---

## 3. 禁則

このリポジトリで作業するうえで、次の7つは守ること。

1. **正典に無い属性を勝手に発明しない。**
   実装上どうしても必要な属性・状態・区分が正典に無い場合、**そこで手を止めて `docs/open-questions.md` に書く**。黙って補うと、実装だけが知っている仕様が生まれる。
   （実例：資料枠の「提出方式」は正典に無いまま、過去のスパイクが `kind: "form" | "link"` を独自に補っていた → `open-questions.md` H-2）

2. **`docs/` の内容を他のファイルへ転記しない。**
   参照は**ファイル名＋見出し**で指す（例：「`requirements.md` §3-4 のとおり」）。コピーした瞬間に二重管理が始まる。

3. **`docs/decisions.md` は追記のみ。過去の行を書き換えない。**
   決定を変更するときは**新しい行に【変更】として書き、旧決定を明記する**（2026-07-26 に確立した運用）。履歴を消さない。

4. **決定の完了定義：`docs/decisions.md` に1行入るまで、それは決定ではない。**
   議論で結論が出た時点ではまだ決定ではない。ログに書かれて初めて決定になる。

5. **未決の上に実装するときは、フォールバック（Stage 1）で作る。**
   例：未決#10（Drive 統合）が未決着の間は、**リンク登録**で成立する形で実装する。受け入れ基準は Stage 1 で満たせるように書かれている。

6. **確度が `暫定` のファイルを、決定の根拠として引用しない。**
   参照するのは自由だが、そこに書いてあることを「決まっている」と扱わない。
   現時点の該当：**`docs/design/` 配下のうち `04-screen.md` を除く全ファイル**（基本設計書。レビュー未了）
   2026-09-03 変更：それ以前は `docs/screens.md` が該当だった。同日に `docs/design/04-screen.md` へ吸収し、暫定性もそのまま引き継いだ。
   2026-09-18 変更：`docs/design/04-screen.md` が鈴木のレビューを終えて確度 `確定` へ昇格したため、該当から外れた（`docs/decisions.md` 同日の2行）。**確度の正は各ファイル冒頭の確度欄であって、この行ではない。** ここは「いま誰が該当するか」の目次にすぎないので、**確度を動かしたら同じ作業の中でこの行も直す**（今回、確度の記述が9箇所に散っていたことが露呈した）。

7. **`main` へ直接 push しない。変更は PR と承認 1 名を経る。確度を上げる変更も同じで、主担当であっても例外にしない。**
   自分の書いたものを、自分だけで正典に入れない。**承認は内容を検証したうえで出す** — 形式だけの承認は、禁則4 の「`decisions.md` に 1 行入って初めて決定」という完了定義を空洞化させる。
   **確度の昇格を名指しするのは、それが設計判断ではなく「チーム全員がこの文書を決定の根拠として引用してよい」という保証だから**（禁則6）。設計そのものは主担当が決めてよいが、**保証はチーム全体の資産で、一人では成立しない。** あわせて、**昇格の PR には「どの論点を解いた結果の確定か」を書く** — 一部の論点だけ解いて上げると、確定の重みが検証していない本文にまで及ぶ。
   `main` はブランチ保護でも塞いである（**PR 必須・承認 1 名・admin も対象**。ほかに会話の解決必須・承認の自動失効などが入っているが、**設定値の正は GitHub のブランチ保護であって、この行ではない** — 全部を書き写すと同期が手作業になる）。**この行は、その設定の根拠を文書に置くためにある。** 設定は変えられるが、合意はここに残る。**運用の合意そのものを変えたら、この行も同じ作業の中で直す。**
   **レビューへ渡す問いは、PR 本文ではなく review thread（差分へのコメント）に置く。** 本文に書いた問いは未回答のままマージできてしまう — 実際 PR #29 の 2 件はそうなった。review thread に置けば、保護設定の「会話の解決必須」が機械的に効く。
   （実例は 2 件ある。**どちらも保護を有効化した 2026-09-18 より前の出来事で、片方は水戸自身のもの**。`main` の first-parent 履歴でマージコミットでないものを数えると、7 月の初期構築を除いて `642c3e4`・`d3e144a` の 2 件になる。
   ① 2026-09-15、**`AGENTS.md` の新設そのものが PR を経ず `main` へ直接入った**（`642c3e4`・水戸）。**この禁則を載せているファイルが、この禁則を破って作られている。**
   ② 2026-09-18、`docs/design/04-screen.md` の確度昇格が PR を経ず `main` へ直接入った（`d3e144a`・鈴木）。**確度が「確定」へ上がるのと同時に誤った確定注記が入り、禁則6 の引用制限が外れたことで、誤りがそのまま「引用してよい根拠」になった。** 訂正に PR #29 を要した。**加えて、この昇格が解いたのは「粒度・分割と順序・ロール差分」の 3 論点であって SC-01〜SC-20 の本文ではないが、禁則6 の引用制限はファイル全体から外れた** → `docs/decisions.md` 2026-09-18）

---

## 4. ファイルの共通ヘッダ

`docs/` 配下の全ファイルは、冒頭に次のヘッダを持つ。**新しいファイルを作るときも必ず付ける。**

```markdown
> **位置づけ**：<誰の、どんな問いに答えるファイルか — 1行>
> **確度**：確定 ／ 派生 ／ 暫定（レビュー未了・鵜呑みにしない）
> **正典**：このファイル ／ 派生（元：<パス>）
> **更新のしかた**：上書き ／ 追記のみ
> **最終更新**：YYYY-MM-DD（更新者）
```

**「正典」と「確度」は別の問い。** 前者は「どこから来たか」、後者は「どれだけ信じてよいか」。正しく導出されているが誰もレビューしていない文書（＝派生かつ暫定）が実在するため、混ぜない。

**`最終更新` は 1 件だけ書く。過去の更新を `／` で連ねない。**
変更の履歴は `git log --oneline -- <ファイル>` が持っている。ヘッダに履歴を積むと、**同じ内容が 2 箇所に出る**（`docs/README.md` §2 でいう二重管理）うえに、**実害としてマージが壊れる** — 1 ファイルにつき全員が同じ 1 行を書き換えるので、**2 本以上の PR が同じファイルを触れば必ず衝突する。**

> 実測（2026-09-21）：オープンな 4 本（#23・#25・#27・#28）のコンフリクトは、`decisions.md` の 1 件を除いて**全部この行だった**。連結は `open-questions.md` 7 件・`00-conventions.md` 4 件・`README.md` 3 件まで伸びていた。

**どうしてもファイル内に履歴を残したい場合だけ**、ファイル**末尾**に `## 変更履歴` 節を置き、**1 行 1 エントリ**で追記する。行単位なら git が自動でマージできるので衝突しない。ヘッダには積まない。

**既存の連結は、そのファイルを次に触る PR が 1 件へ削る。** 一括で直すと、いま開いている PR 全部と衝突して意味がない。

---

## 5. 技術構成

**企画書 §2-3・§3 を基礎に、そこに記載の無い採用を `docs/decisions.md` を出典として加えたもの。** 本表は企画書の上位集合であり、そのままの写しではない。**具体的なバージョンは Ph.2 で `package.json` と Docker に固定する**（それまでは最新安定版・Node.js は LTS）。

**「出典」列は、その採用がどこで決まったかを示す。** 企画書は凍結済み Ph.0 成果物なので書き換えられない。**企画書に無い採用は `docs/decisions.md` を出典として本表に持たせる**（2026-09-06・H-18 決着）。

| 層 | 採用 | 出典 |
| --- | --- | --- |
| 言語 | TypeScript | 企画書 §2-3 |
| フレームワーク | Next.js（App Router） | 企画書 §2-3 |
| ORM | Prisma | 企画書 §2-3 |
| DB | PostgreSQL | 企画書 §2-3 |
| 認証 | Auth.js（Google Provider・学校 Workspace アカウント） | 企画書 §2-3 |
| スタイル | Tailwind CSS 4 | `docs/decisions.md`（2026-09-06） |
| UI 最適化 | React Compiler（`next.config.ts` の `reactCompiler: true`） | `docs/decisions.md`（2026-09-06） |
| パッケージマネージャ | pnpm | `docs/decisions.md`（2026-09-06） |
| 開発環境 | Docker | 企画書 §3-1 |

**認証（Auth.js ＋ 学校 Google アカウントで OAuth が通るか）が最大の技術リスク。** 詳細は `docs/requirements.md` §5。
その手前にあったロール解決（H-10）は **2026-09-05 に先生ホワイトリスト方式で決着**した（`docs/decisions.md`）ので、**残る技術リスクは OAuth 自体の可否だけ**になっている。

---

## 6. 役割分担

| 担当 | 領域 |
| --- | --- |
| 水戸 | リーダー・要件・ステークホルダー対応・**実装が設計どおりかの検証** |
| 蒲山 | バックエンド・インフラ・DB・API の**実装**・**技術検証**（実現可否と方式の選定） |
| 鈴木 | フロントエンド・画面 UI/UX・タスク／スケジュール管理 |

> **「検証」という語が 2 か所に出るが、別の作業を指す。**
> 蒲山の検証は「**作れるかを確かめる**」（技術検証。例：学校アカウントで OAuth が通るか・PDF 生成の実現手段）。
> 水戸の検証は「**作ったものが設計どおりかを確かめる**」（適合性の検証）。
> `docs/` で「検証」と書くときは、**どちらの意味かが読み手に分かる形で書く**。

テストは全員担当。**設計書発表会は 2026-10-15。**
