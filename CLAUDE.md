# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## プロジェクト概要

YSE Compass — 学校の卒業制作・発表会運営を支える学内専用Webシステム。発表会・資料枠・提出状況・コメントなどの**メタデータ**を管理し、資料ファイルの実体は Google Drive 等の外部に置く（実体レス原則）。唯一の例外が概要集で、概要は構造化フォーム入力としてシステム内に保持し、統一レイアウトで PDF 化する（2026-07-26 決定）。

現在は要件定義フェーズ（Ph.1）。`src/` は Next.js の雛形のみで機能実装は未着手。設計・実装の前に必ず [docs/requirements.md](docs/requirements.md) を読むこと。

## コマンド

パッケージマネージャは **pnpm**（npm / yarn は使わない）。

- `pnpm dev` — 開発サーバー起動（http://localhost:3000）
- `pnpm build` — 本番ビルド
- `pnpm lint` — ESLint 実行
- `pnpm exec tsc --noEmit` — 型チェック（専用スクリプトは未定義）

テストフレームワークは未導入（導入時に本書へ追記する）。

## ドキュメントの正典（SSoT）

- 要件定義書の正典は **Notion**。`docs/requirements.md` はそのミラーであり、**ユーザーの指示があったときのみ**同期・更新する。Claude が独断で編集しない
- 企画書（Ph.0 の正典）は凍結済みでリポジトリには置かない。スコープの記述が食い違う場合は要件定義書を優先する
- 未決事項が決着したら要件定義書 §2「決定事項ログ」へ追記し、履歴は消さない運用

## 確定済みのスコープ境界（実装判断の前提）

詳細と根拠は docs/requirements.md §1〜§2。以下に反する設計・実装を提案しないこと：

- **状態遷移（資料公開・当日進行・年度アーカイブ）は先生の明示操作を正とし、時刻・条件による自動遷移は持たない** — 2026-07-26 に設計原則として昇格。個別機能より優先する横断ルール
- 資料ファイルの実体は保管しない — 管理対象はリンク・種別・提出状態などのメタデータ（概要のみ例外・下記）
- 学内専用（一般公開なし）。ゲーム・Web システムの実行ホスティングもしない
- 提出状態は「未提出 / 提出済み」の 2 値。差し替えは上書きで版履歴を持たない。差し替えの終端は年度アーカイブ
- 締切はソフトデッドライン — 超過しても提出・差し替えでき、遅延として可視化する（ブロックしない）。遅延は第 3 の状態ではなく、提出日時と締切の比較による導出表示
- 提出フォームの検証は URL の形式のみ。リンクのアクセス可否は検証しない
- 概要集の自動生成はスコープ内 — 概要は構造化フォーム入力、システム定義の統一レイアウト（コード定義・1 種類）で発表順に束ねて PDF 化。ファイルの取り込み・変換・レイアウト編集 UI は作らない。画像対応は保留
- 資料公開は先生の明示操作（公開ボタン）による発表会単位の一括公開。公開前は聴く生徒に非表示
- 通知送信（メール・プッシュ）はしない — 締切・未提出の可視化はシステム内表示のみ
- コメントのリアルタイム同期はしない。コメント・いいねのアンカーは発表（チーム × 発表会）単位
- 発表会の回種別は固定 4 種（企画／設計／試作／最終）で順序つき・コード定義。発表会作成の正規入口は「年度一括セットアップ」で、任意種別・任意個数の作成 UI は持たない
- システム内プレビューはしない — 資料閲覧はリンク先（Drive 等）へ別タブ遷移
- アーカイブ検索の対象はメタデータ＋概要の構造化データ（本文・使用技術欄）。Drive 実体の本文検索・独立タグ機能・リンク切れの自動検知は持たない
- 卒業生の公開許可は作品（チーム × 年度）単位・同意者はチーム代表者・既定は非表示。許可のない作品はアーカイブにも検索結果にも出さない
- データ保持は無期限。削除機能を持たない
- 表示名は実名（学校アカウントのフルネーム）。匿名化機能は持たない

要件定義書 §6 の未決事項に依存する箇所（提出フローの Drive 自動化 #10、当日進行状態の伝播方式 #4 など）は、既定（Stage 1: リンク登録フォーム／リアルタイム同期なし）を前提に実装し、未決の答えを先取りしない。概要集の PDF 生成手段（サーバーサイド生成 / 印刷用ビュー）も未確定のため、受け入れ基準を手段で縛らない。

## 技術スタック

- Next.js 16（App Router）+ React 19 + TypeScript（strict）+ Tailwind CSS 4
- React Compiler 有効（`next.config.ts` の `reactCompiler: true`）
- パスエイリアス: `@/*` → `./src/*`
- 認証は Auth.js + 学校 Google アカウント（Workspace）OAuth を予定。実現可否が最大の技術リスクで、検証は設計フェーズ後半に委譲（要件定義書 §5）
- UI 言語は日本語（`layout.tsx` は `lang="ja"`）

## 規約

- ドキュメント・コミットメッセージは日本語。コミットメッセージには `docs:` などの Conventional Commits 形式のプレフィックスを付ける
