services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-yse_compass}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-yse_compass}
      POSTGRES_DB: ${POSTGRES_DB:-yse_compass}
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-yse_compass}"]
      interval: 5s
      timeout: 5s
      retries: 10

  app:
    build:
      context: .
      target: dev
    command: pnpm exec next dev --webpack
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-yse_compass}:${POSTGRES_PASSWORD:-yse_compass}@db:5432/${POSTGRES_DB:-yse_compass}
      WATCHPACK_POLLING: "true"
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    depends_on:
      db:
        condition: service_healthy

volumes:
  db_data:
