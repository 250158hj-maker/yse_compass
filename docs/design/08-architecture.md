# 08. 方式設計

> **位置づけ**：どの技術で、どんな層構成で、どこに置いて動かすかを答える
> **確度**：**暫定（レビュー未了）**
> **正典**：このファイル（**技術スタックの一覧は `../../CLAUDE.md` §5**）
> **更新のしかた**：上書き
> **主担当**：蒲山
> **最終更新**：2026-09-03（水戸）

## この章が答える問い

**要件を成立させる技術的な骨格は何か。** 機能設計（03）が「何をするか」なら、この章は「何の上で動くか」。

## 書くもの

| # | 成果物 | 形式 |
| --- | --- | --- |
| 8-1 | 技術スタックと採用理由 | 表（層 ／ 採用 ／ バージョン方針 ／ **採用理由** ／ 出典） |
| 8-2 | システム構成図 | 図（ブラウザ ／ Next.js ／ DB ／ Google の関係） |
| 8-3 | アプリケーション層構成 | 図（App Router ／ Server Component ／ Server Action ／ データアクセス層の責務分界） |
| 8-4 | 状態管理とデータ取得の方針 | Server Component 優先 ／ Client Component の範囲 ／ 再検証のタイミング |
| 8-5 | 当日進行の伝播方式 | ポーリング間隔と、その根拠（同時 120 名） |
| 8-6 | 開発・実行環境 | Docker 構成 ／ 環境変数 ／ ローカル起動手順 |
| 8-7 | デプロイ構成 | 本番環境が確定した後に書く |

## 書かないもの

| 書かないこと | 参照先 |
| --- | --- |
| 技術スタックの一覧そのもの | `../../CLAUDE.md` §5（**再掲しない。採用理由だけ書く**） |
| 性能・可用性の目標値 | `09-nfr.md` |
| Google との IF | `07-interface.md` |

> **バッチ・スケジューラの節は置かない。** 設計原則「状態遷移は先生の明示操作を正とし、時刻・条件による自動遷移は持たない」（`../requirements.md` §3 冒頭）の帰結として、設計に存在しない。

## 埋まる条件

**書ける。8-7 のみ保留。**

| 障害 | 内容 | 影響する箇所 |
| --- | --- | --- |
| **#4** | 当日進行の伝播方式。ただし**同時 120 名が確定したため、判断基準を適用すればポーリングで即断できる状態** | 8-5（先行方針＝手動リロード） |
| **本番環境が未確定** | 学校が検討中。**判断期限（8/27）を超過している** | 8-7 |
| **保留** | PDF 生成の実現手段（サーバー生成 or 印刷用ビュー）。実装コストが大きく異なる | 8-1（依存ライブラリが変わる） |
| **M-1（監査）** | `CLAUDE.md` §5 は ORM=**Prisma** だが、実装は `pg` 直。**逸脱が `decisions.md` に無記録** | 8-1（どちらが正なのか確定していない） |

> **8-5 は「決められるのに決めていない」典型。** #4 は材料が揃っており、`decisions.md` に1行入れれば閉じる。

## 現在の状態

**未着手。ただし障害はほぼ無く、いま書ける2章のうちの1つ。**
