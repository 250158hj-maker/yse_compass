# 09. 非機能設計

> **位置づけ**：性能・可用性・セキュリティ・運用の要件を、**どの仕組みで満たすか**を答える
> **確度**：**暫定（レビュー未了）**
> **正典**：このファイル（**要件そのものは `../requirements.md` §4**）
> **更新のしかた**：上書き
> **主担当**：蒲山・水戸
> **最終更新**：2026-09-05（水戸・H-10／G-3 決着を反映／G-4 起票を反映）

## この章が答える問い

`../requirements.md` §4 が「**何を満たすか**」に答えるのに対し、この章は「**どうやって満たすか**」に答える。

## 書くもの

| # | 成果物 | 形式 |
| --- | --- | --- |
| 9-1 | 性能設計 | 想定負荷（同時 120 名）／ 目標応答時間 ／ 想定クエリと対策 |
| 9-2 | 可用性・バックアップ設計 | 停止許容時間 ／ バックアップ方式・世代・復旧手順 |
| 9-3 | 認証設計 | `07-interface.md` の OAuth を受けたセッション管理 |
| 9-4 | **認可設計** | **どの層で担保するか**（画面表示制御／Server Action の入口／DB） ／ 権限判定の実装方針 |
| 9-5 | 個人情報の扱い | 実名表示の原則（2026-07-24）を、どの画面・どの出力で適用するか |
| 9-6 | ログ・監査証跡 | 何を記録し、何を記録しないか |
| 9-7 | アクセシビリティ・対応環境 | 対応ブラウザ・端末 |

> **9-4 が本章の中心。** 監査（2026-09-03）で、実装の認可がクライアント側の表示制御のみで、**サーバー側の担保が無く、かつどこに担保を置くかが正典にも書かれていない**ことが確認されている（→ **H-16**）。

## 書かないもの

| 書かないこと | 参照先 |
| --- | --- |
| 非機能要件そのものと根拠 | `../requirements.md` §4 |
| 技術スタックの選定 | `08-architecture.md` |
| 運用の手順 | `10-operation.md` |

## 埋まる条件

**部分的にしか書けない。原因の多くは「設計の未決」ではなく「要件の欠落」。**

| 障害 | 内容 | 影響する箇所 |
| --- | --- | --- |
| **§4 の認可原則が TODO** | 認可（ロール別アクセス制御）の**原則が未定義**。**H-10（ロール解決）は 2026-09-05 決着**したので前提は外れ、残る障害は **H-16**（どの層で担保するか） | 9-4（章の中心） |
| **可用性・バックアップが 0 件（→ G-2）** | 「保持期間は無期限・削除機能を持たない」と決めているのに、**バックアップ要件が存在しない**（整合していない） | 9-2 |
| **対応環境が正典に無い** | 対応ブラウザ・端末の要件が `docs/` に無い | 9-7 |
| **#8** | 先生ロールの親子二層構造の要否 | 9-4（先行方針＝単層） |
| **保留** | **ポーリング間隔の値**。方式はポーリングで確定（2026-09-04・#4 決着）だが、**許容遅延が正典に無い**ため間隔が決まらない | 9-1 |
| **G-4** | 概要集生成（§3-3）が §4 性能の 2 区分のどちらにも書かれていない（G-3 決着時の列挙漏れ）。**PDF 生成は 1 リクエストで数十ページを組む処理**で、検索やコメント投稿と同じ枠に括ってよいかは別の判断 | 9-1 |

> **9-2・9-7 は、この章では埋められない。** 要件が存在しないので、**`requirements.md` §4 に要件を足す作業が先**。設計書で先に書くと、要件を設計が後から追認する形になる（`00-conventions.md` §3-1 の逆流）。
> **9-1（性能）は 2026-09-05 に書けるようになった。** §4 へ応答時間目標（当日進行系 p95 ≦ 1 秒／それ以外 p95 ≦ 3 秒）が入った（G-3 決着）。**ポーリング間隔の値のみ保留**。ただし**概要集生成（§3-3）はどちらの区分にも列挙されていない**（G-4）ので、9-1 に §3-3 の行を書くのは本項の決着後。

## 現在の状態

**未着手。** **06 データ設計とは独立しているので、並行して進められる。** ただし着手の前に要件側の補充が要る。
